if(NOT ANDROID)
	file(GLOB_RECURSE resilsourceFiles ./src-IL/src/*.cpp)
	file(GLOB_RECURSE resilsourceFilesc ./src-IL/src/*.c)
	file(GLOB_RECURSE resilheaderFiles ./include/*.h)
	file(GLOB_RECURSE resilheaderFiles2 ./src-IL/include/*.h)
	include_directories(./src-IL/include)
	include_directories(./include)
	if(WIN32)
		tng_library(resil ${resilsourceFiles} ${resilheaderFiles} ${resilheaderFiles2} ${resilsourceFilesc} )
	else()
		if(share_library)
			tng_shared_library(resil ${resilsourceFiles} ${resilheaderFiles} ${resilheaderFiles2} ${resilsourceFilesc} )
		else()
			tng_library(resil ${resilsourceFiles} ${resilheaderFiles} ${resilheaderFiles2} ${resilsourceFilesc} )
		endif()
	endif()
endif()
