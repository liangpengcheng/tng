if(NOT ANDROID)
	file(GLOB_RECURSE glewFilesc ./*.c)
	file(GLOB_RECURSE glewHeaderFiles ./*.h)

	if(WIN32)
		tng_library(GLEW ${glewFilesc} ${glewHeaderFiles} )
	else()
		if(share_library)
			tng_shared_library(GLEW ${glewFilesc} ${glewHeaderFiles} )
		else()
			tng_library(GLEW ${glewFilesc} ${glewHeaderFiles} )
		endif()
	endif()
endif()
